diff -urN original/eval.sh modified/eval.sh
--- original/eval.sh	2026-02-24 03:11:52.796836777 -0500
+++ modified/eval.sh	2026-02-24 03:13:54.857421147 -0500
@@ -1,87 +1,74 @@
-PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
-    --model diffllm \
-    --model_args pretrained=Dream-org/Dream-v0-Instruct-7B,trust_remote_code=True,max_new_tokens=128,diffusion_steps=128,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy" \
-    --tasks mmlu_generative \
-    --device cuda \
-    --batch_size 1 \
-    --num_fewshot 4 \
-    --output_path output_reproduce/mmlu \
-    --log_samples --confirm_run_unsafe_code \
-    --apply_chat_template
-
-PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
-    --model diffllm \
-    --model_args pretrained=Dream-org/Dream-v0-Instruct-7B,trust_remote_code=True,max_new_tokens=128,diffusion_steps=128,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy" \
-    --tasks mmlu_pro \
-    --device cuda \
-    --batch_size 1 \
-    --num_fewshot 4 \
-    --output_path output_reproduce/mmlu_pro \
-    --log_samples --confirm_run_unsafe_code \
-    --apply_chat_template
+models=(
+    "<YOUR_HF_USERNAME>/Dream-v0-Instruct-7B-SM"
+    "<YOUR_HF_USERNAME>/Dream-Coder-v0-Instruct-7B-SM"
+    "<FINETUNED-SOFTMASKING-MODEL-PATHS>"
+    "<FINETUNED-BINARY-MODEL-PATHS>"
+)
+model=${models[2]}
+softmasking=True
 
-PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
+HF_ALLOW_CODE_EVAL=1 PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
     --model diffllm \
-    --model_args pretrained=Dream-org/Dream-v0-Instruct-7B,trust_remote_code=True,max_new_tokens=256,diffusion_steps=256,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy" \
-    --tasks gsm8k_cot \
+    --model_args pretrained=$model,trust_remote_code=True,max_new_tokens=768,diffusion_steps=768,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy",softmasking=$softmasking \
+    --tasks humaneval_instruct \
     --device cuda \
     --batch_size 1 \
     --num_fewshot 0 \
-    --output_path output_reproduce/gsm8k \
+    --output_path output_reproduce/humaneval \
     --log_samples --confirm_run_unsafe_code \
     --apply_chat_template
 
-PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
+HF_ALLOW_CODE_EVAL=1 PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
     --model diffllm \
-    --model_args pretrained=Dream-org/Dream-v0-Instruct-7B,trust_remote_code=True,max_new_tokens=512,diffusion_steps=512,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy" \
-    --tasks minerva_math \
+    --model_args pretrained=$model,trust_remote_code=True,max_new_tokens=512,diffusion_steps=512,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy",softmasking=$softmasking \
+    --tasks mbpp_instruct \
     --device cuda \
     --batch_size 1 \
     --num_fewshot 0 \
-    --output_path output_reproduce/math \
+    --output_path output_reproduce/mbpp \
     --log_samples --confirm_run_unsafe_code \
     --apply_chat_template
 
-PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
+HF_ALLOW_CODE_EVAL=1 PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
     --model diffllm \
-    --model_args pretrained=Dream-org/Dream-v0-Instruct-7B,trust_remote_code=True,dtype="bfloat16" \
-    --tasks gpqa_main_n_shot \
+    --model_args pretrained=$model,trust_remote_code=True,max_new_tokens=768,diffusion_steps=768,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy",softmasking=$softmasking \
+    --tasks humaneval_plus_instruct \
     --device cuda \
     --batch_size 1 \
-    --num_fewshot 5 \
-    --output_path output_reproduce/gpqa \
+    --num_fewshot 0 \
+    --output_path output_reproduce/humaneval_plus \
     --log_samples --confirm_run_unsafe_code \
     --apply_chat_template
 
 HF_ALLOW_CODE_EVAL=1 PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
     --model diffllm \
-    --model_args pretrained=Dream-org/Dream-v0-Instruct-7B,trust_remote_code=True,max_new_tokens=768,diffusion_steps=768,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy" \
-    --tasks humaneval_instruct \
+    --model_args pretrained=$model,trust_remote_code=True,max_new_tokens=512,diffusion_steps=512,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy",softmasking=$softmasking \
+    --tasks mbpp_plus_instruct \
     --device cuda \
     --batch_size 1 \
     --num_fewshot 0 \
-    --output_path output_reproduce/humaneval \
+    --output_path output_reproduce/mbpp_plus \
     --log_samples --confirm_run_unsafe_code \
     --apply_chat_template
 
-HF_ALLOW_CODE_EVAL=1 PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
+PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
     --model diffllm \
-    --model_args pretrained=Dream-org/Dream-v0-Instruct-7B,trust_remote_code=True,max_new_tokens=1024,diffusion_steps=1024,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy" \
-    --tasks mbpp_instruct \
+    --model_args pretrained=$model,trust_remote_code=True,max_new_tokens=256,diffusion_steps=256,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy",softmasking=$softmasking \
+    --tasks gsm8k_cot \
     --device cuda \
     --batch_size 1 \
     --num_fewshot 0 \
-    --output_path output_reproduce/mbpp \
+    --output_path output_reproduce/gsm8k \
     --log_samples --confirm_run_unsafe_code \
     --apply_chat_template
 
 PYTHONPATH=. accelerate launch --main_process_port 12334 -m lm_eval \
     --model diffllm \
-    --model_args pretrained=Dream-org/Dream-v0-Instruct-7B,trust_remote_code=True,max_new_tokens=1280,diffusion_steps=1280,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy" \
-    --tasks ifeval \
+    --model_args pretrained=$model,trust_remote_code=True,max_new_tokens=512,diffusion_steps=512,dtype="bfloat16",temperature=0.1,top_p=0.9,alg="entropy",softmasking=$softmasking \
+    --tasks math500 \
     --device cuda \
     --batch_size 1 \
     --num_fewshot 0 \
-    --output_path output_reproduce/ifeval \
+    --output_path output_reproduce/math \
     --log_samples --confirm_run_unsafe_code \
     --apply_chat_template
diff -urN original/.gitignore modified/.gitignore
--- original/.gitignore	2026-02-24 03:11:52.674081000 -0500
+++ modified/.gitignore	1969-12-31 19:00:00.000000000 -0500
@@ -1,26 +0,0 @@
-env
-*.pyc
-output/
-output5/
-data/
-lm_cache
-.idea
-build
-dist
-*.egg-info
-venv
-.venv/
-.vscode/
-temp
-__pycache__
-.ipynb_checkpoints
-temp
-test_logs/
-# IPython
-profile_default/
-ipython_config.py
-# don't track (the default location of) the cached requests
-lm_eval/caching/.cache
-# don't track files created by wandb
-wandb
-examples/wandb
diff -urN original/lm_eval/models/diffllm.py modified/lm_eval/models/diffllm.py
--- original/lm_eval/models/diffllm.py	2026-02-24 03:11:55.515009000 -0500
+++ modified/lm_eval/models/diffllm.py	2026-02-24 03:13:55.440103000 -0500
@@ -22,10 +22,20 @@
 from lm_eval.api.registry import register_model
 from lm_eval.models.utils import Collator, get_dtype
 
+import json
+from huggingface_hub import hf_hub_download
+from peft import PeftModel, PeftConfig, PeftType
+
 eval_logger = logging.getLogger(__name__)
 T = TypeVar("T", bound="LM")
 
-
+def is_lora_adapter(model_path):
+    try:
+        config = PeftConfig.from_pretrained(model_path)
+        return config.peft_type == PeftType.LORA
+    except Exception:
+        return False
+    
 def empty_cache_by_memory(threshold_gb=70):
     """
     Empty CUDA cache if allocated memory exceeds threshold
@@ -61,10 +71,14 @@
         trust_remote_code: Optional[bool] = True,
         parallelize: Optional[bool] = False,
         autogptq: Optional[Union[bool, str]] = False,
+        softmasking: Optional[bool] = False,
         **kwargs,
     ) -> None:
         super().__init__()
 
+        # softmasking
+        self.softmasking = softmasking
+
         # prepare for parallelism
         assert isinstance(device, str)
         assert isinstance(pretrained, str)
@@ -205,16 +219,47 @@
         return self._world_size
 
     def _create_model_and_tokenizer(self, pretrained, dtype, trust_remote_code):
-        self.model = (
-            transformers.AutoModel.from_pretrained(
+
+        # Basic to ensure softmasking doesnt happen unless specified
+        self.th_kwargs = {
+            "transparency_alg": "none"
+        }
+        if is_lora_adapter(pretrained):
+            print("LoRA adapter detected.")
+            if self.softmasking:
+                path = hf_hub_download(pretrained, "transparency_config.json")
+                with open(path) as f:
+                    self.th_kwargs = json.load(f)
+                    # TODO: Can patch if want different params than from file
+                    # self.th_kwargs["mixinputs_k"] = 1
+                    print("Transparency parameters", self.th_kwargs)
+                
+            config = PeftConfig.from_pretrained(pretrained)
+            base_model = transformers.AutoModel.from_pretrained(
+                                                    config.base_model_name_or_path,
+                                                    torch_dtype=get_dtype(dtype),
+                                                    trust_remote_code=trust_remote_code,
+                                                )
+
+            print("Merging adapter into base weights with merge_and_unload()")
+            peft_model = PeftModel.from_pretrained(base_model, pretrained)
+            self.model = peft_model.merge_and_unload().to(dtype=get_dtype(dtype))
+
+            # Tokenizer should come from the base model, not the adapter repo
+            tokenizer_source = config.base_model_name_or_path
+
+        else:
+            print("Base model detected.")
+            self.model = transformers.AutoModel.from_pretrained(
                 pretrained,
                 torch_dtype=get_dtype(dtype),
                 trust_remote_code=trust_remote_code,
             )
-            .eval()
-        ).to(self.device)
+            tokenizer_source = pretrained
+
+        self.model.eval().to(self.device)
         self.tokenizer = transformers.AutoTokenizer.from_pretrained(
-            pretrained, trust_remote_code=trust_remote_code
+            tokenizer_source, trust_remote_code=trust_remote_code
         )
 
     def tok_decode(self, tokens, skip_special_tokens=True):
@@ -284,6 +329,7 @@
             top_k=self.top_k,
             alg=self.alg,
             alg_temp=self.alg_temp,
+            **self.th_kwargs
         )
 
         # decode
diff -urN original/lm_eval/tasks/humaneval/humaneval_plus_instruct.yaml modified/lm_eval/tasks/humaneval/humaneval_plus_instruct.yaml
--- original/lm_eval/tasks/humaneval/humaneval_plus_instruct.yaml	1969-12-31 19:00:00.000000000 -0500
+++ modified/lm_eval/tasks/humaneval/humaneval_plus_instruct.yaml	2026-02-24 03:13:56.556625784 -0500
@@ -0,0 +1,12 @@
+include: humaneval.yaml
+task: humaneval_plus_instruct
+dataset_path: evalplus/humanevalplus
+doc_to_text: "Write a solution to the following problem and make sure that it passes the tests:\n```{{prompt}}"
+gen_prefix: "Here is the completed function:\n```python\n{{prompt}}\n"
+filter_list:
+  - name: "create_test"
+    filter:
+      - function: "custom"
+        filter_fn: !function utils.build_predictions_instruct
+metadata:
+  version: 2.0
\ No newline at end of file
diff -urN original/lm_eval/tasks/humaneval/humaneval.yaml modified/lm_eval/tasks/humaneval/humaneval.yaml
--- original/lm_eval/tasks/humaneval/humaneval.yaml	2026-02-24 03:11:59.491355960 -0500
+++ modified/lm_eval/tasks/humaneval/humaneval.yaml	2026-02-24 03:13:56.330013507 -0500
@@ -17,6 +17,7 @@
     - "\n#"
     - "\nif"
     - "\nprint"
+    - "<|im_end|>"
   max_gen_toks: 1024
   do_sample: false
 repeats: 1
diff -urN original/lm_eval/tasks/humaneval/utils.py modified/lm_eval/tasks/humaneval/utils.py
--- original/lm_eval/tasks/humaneval/utils.py	2026-02-24 03:12:00.404701448 -0500
+++ modified/lm_eval/tasks/humaneval/utils.py	2026-02-24 03:13:56.586578660 -0500
@@ -1,10 +1,11 @@
 import evaluate as hf_evaluate
 
 from lm_eval.tasks.humaneval.sanitize_utils import sanitize
-
+import os, uuid
 
 try:
-    compute_ = hf_evaluate.load("code_eval")
+    exp_id = f"{os.getenv('LSB_JOBID','nojob')}-{uuid.uuid4().hex[:8]}"
+    compute_ = hf_evaluate.load("code_eval", experiment_id=exp_id)
     test_cases = ["assert add(2, 3)==5"]
     candidates = [["def add(a,b): return a*b"]]
     results = compute_.compute(references=test_cases, predictions=candidates, k=[1])
diff -urN original/lm_eval/tasks/minerva_math/minerva_math_500.yaml modified/lm_eval/tasks/minerva_math/minerva_math_500.yaml
--- original/lm_eval/tasks/minerva_math/minerva_math_500.yaml	1969-12-31 19:00:00.000000000 -0500
+++ modified/lm_eval/tasks/minerva_math/minerva_math_500.yaml	2026-02-24 03:13:56.882823309 -0500
@@ -0,0 +1,32 @@
+tag:
+  - math_word_problems
+task: math500
+dataset_path: HuggingFaceH4/MATH-500
+process_docs: !function utils.process_docs
+# dataset_name: algebra
+output_type: generate_until
+training_split: train
+test_split: test
+doc_to_text:  !function utils.doc_to_text_500
+process_results: !function utils.process_results
+doc_to_target: "{{answer if few_shot is undefined else solution}}"
+generation_kwargs:
+  until:
+    - "Problem:"
+  do_sample: false
+  temperature: 0
+metric_list:
+  - metric: exact_match
+    aggregation: mean
+    higher_is_better: true
+  - metric: math_verify
+    aggregation: mean
+    higher_is_better: true
+num_fewshot: 0
+metadata:
+  version: 2.0
+dataset_kwargs:
+  trust_remote_code: true
+fewshot_config:
+  sampler: first_n
+  samples: !function utils.list_fewshot_samples
diff -urN original/lm_eval/tasks/minerva_math/utils.py modified/lm_eval/tasks/minerva_math/utils.py
--- original/lm_eval/tasks/minerva_math/utils.py	2026-02-24 03:12:02.452428000 -0500
+++ modified/lm_eval/tasks/minerva_math/utils.py	2026-02-24 03:13:57.022092000 -0500
@@ -29,6 +29,8 @@
 def doc_to_text(doc: dict) -> str:
     return "Problem:" + "\n" + doc["problem"] + "\n\n" + "Solution:"
 
+def doc_to_text_500(doc: dict) -> str:
+    return "Please reason step by step, and put your final answer within \\boxed{{}}. Problem:" + "\n" + doc["problem"] + "\n\n" + "Solution:"
 
 def process_docs(dataset: datasets.Dataset) -> datasets.Dataset:
     def _process_doc(doc: dict) -> dict:
